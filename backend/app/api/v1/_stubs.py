# Placeholder routers — to be implemented in their respective phases
from fastapi import APIRouter

knowledge_router = APIRouter()
sessions_router = APIRouter()
war_room_router = APIRouter()
proposals_router = APIRouter()
